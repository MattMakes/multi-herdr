# Pre-flight: phase skills

CLEAR FOR TAKEOFF. Standalone Rust CLI; no service/auth setup task required.

- Wiring: all five launch builders identified; both worker and fixed-pane paths require integration.
- Behavior: preserve session argument order, scoped Codex rules, Prime daemon ownership and operator auth.
- Contracts: phase fields default to absent; existing brief and ledger records remain readable.
- Configuration: no global config writes; private roots and child-only overlays. Native discovery is checked separately from argv.
- Domain: research/plan/implementation/validation are explicit assignment metadata.
- Credentials: none in the skill bundle; existing harness auth stays in its existing source.
- Gates: four reviewed Cargo checks approved in a temporary store outside the repository. Baseline G2/G4 fail on the known Claude auto-mode assertion; G1/G3 tests are added during implementation.
- Task ownership: skill content, roster/phase contracts and runtime integration are disjoint.
